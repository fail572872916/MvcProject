<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jetpacktest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".androidviewmodule.Main6Activity"></activity>

        <service
            android:name=".saveInstanceState.MyService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".saveInstanceState.Main5Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".testScore.Main4Activity" />
        <activity android:name=".databinding.Main3Activity" />
        <activity android:name=".livedata.Main2Activity" />
        <activity android:name=".viewmodel.MainActivity" />
    </application>

</manifest>